name: Custom actions
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v2

    - name: hello world custom action 
      uses: Devops-Learning-CICD/Coderco-custom@main
      with:  
        who-to-greet:'Coderco Team'